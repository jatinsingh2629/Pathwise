services:
  - type: web
    name: pathwise
    runtime: python
    buildCommand: |
      pip install --upgrade pip setuptools wheel
      pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13   # 👈 ADD THIS
      - key: SECRET_KEY
        generateValue: true
      - key: FLASK_ENV
        value: production
